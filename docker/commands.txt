sudo docker image build -t hello-world-server:1.0 .
sudo docker image build -t hivanco/hello-world-server:1.0 .

sudo docker container run --publish 4001:4001 --detach -e "PROXY_SERVICE_IP=10.11.17.6" --name hws hivanco/hello-world-server:1.0
sudo docker container run --network="host" --detach -e "PROXY_IP=10.11.17.6" --name hws hivanco/hello-world-server:1.0

sudo docker exec -it hws /bin/bash

sudo docker container rm --force hws

sudo docker image ls

sudo docker ps

sudo docker login

sudo docker image tag hello-world-server:1.0 hivanco/hello-world-server:1.0

sudo docker image push hivanco/hello-world-server:1.0

sudo docker save -o hello-world-server.1.0.tar hivanco/hello-world-server:1.0
